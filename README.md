# 五子棋對戰平台 (Gomoku Online)

一個基於 React/Next.js 的高性能五子棋對戰網頁應用程式，具備即時對戰、積分系統、段位排名等功能。

## 📁 專案結構

```
Gomoku_cloud/
├── frontend/          # Next.js 前端應用
│   ├── src/
│   │   ├── app/       # Next.js App Router
│   │   ├── components/# React 元件
│   │   ├── lib/       # 工具函數
│   │   ├── stores/    # Zustand 狀態管理
│   │   ├── types/     # TypeScript 型別定義
│   │   ├── constants/ # 常數配置
│   │   └── services/  # API 和 Socket 服務
│   └── ...
├── backend/           # WebSocket 伺服器
│   ├── src/
│   │   ├── handlers/  # Socket 事件處理器
│   │   ├── services/  # 業務邏輯服務
│   │   ├── types/     # TypeScript 型別定義
│   │   └── constants/ # 常數配置
│   └── ...
└── ...
```

## 🚀 快速開始

### 前置需求

- Node.js 18+
- npm 或 yarn

### 安裝與啟動

#### 1. 安裝前端依賴

```bash
cd frontend
npm install
```

#### 2. 安裝後端依賴

```bash
cd backend
npm install
```

#### 3. 啟動開發伺服器

**啟動後端 WebSocket 伺服器：**
```bash
cd backend
npm run dev
```

**啟動前端開發伺服器：**
```bash
cd frontend
npm run dev
```

前端將在 http://localhost:3000 運行
後端將在 ws://localhost:3001 運行

## 🧪 測試

**前端測試：**
```bash
cd frontend
npm test
```

**後端測試：**
```bash
cd backend
npm test
```

## 🎮 功能特色

- ♟️ **即時對戰** - 基於 WebSocket 的即時多人對戰
- 🏆 **段位系統** - 銅、銀、金、白金、鑽石、大師、頂級七個段位
- 📊 **積分系統** - ELO 積分計算
- 🎯 **隨機匹配** - 根據積分進行智能匹配
- 🏠 **房間系統** - 支援公開/私人房間
- 💬 **即時聊天** - 對戰中的文字和表情交流
- ↩️ **悔棋功能** - 經對方同意的悔棋機制
- 🔄 **再來一局** - 快速重新開始遊戲

## 🛠️ 技術棧

### 前端
- **框架:** Next.js 14 (App Router)
- **語言:** TypeScript
- **樣式:** Tailwind CSS
- **狀態管理:** Zustand
- **即時通訊:** Socket.IO Client

### 後端
- **運行環境:** Node.js
- **語言:** TypeScript
- **即時通訊:** Socket.IO
- **架構:** Clean Architecture

## 📦 部署

### Vercel (前端)
```bash
cd frontend
vercel
```

### Render (後端)
配置 `render.yaml` 或使用 Render Dashboard 部署

## 📄 授權

MIT License
